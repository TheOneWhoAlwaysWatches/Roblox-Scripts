-- ⚡ ULTIMATE+ NO COOLDOWN NUKE EDITION ⚡
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local Stepped = RunService.Stepped
local Heartbeat = RunService.Heartbeat

-- Patch wait() for faster yields
hookfunction(wait, function() return Stepped:Wait() end)

-- === 1️⃣ Cooldown nuker (constant patch every frame)
local function patchUpvalues()
    for _, v in pairs(getgc(true)) do
        if typeof(v) == "function" and islclosure(v) then
            for i = 1, debug.getinfo(v).nups do
                local name = debug.getupvalue(v, i)
                if type(name) == "string" and name:lower():find("cooldown") then
                    debug.setupvalue(v, i, 0)
                end
            end
        end
    end
end

task.spawn(function()
    while true do
        patchUpvalues()
        Stepped:Wait()
    end
end)

-- === 2️⃣ Remote spammer (parallel burst)
local function spamToolRemotes(target)
    local char = LocalPlayer.Character
    if not char then return end
    local tool = char:FindFirstChildOfClass("Tool")
    if not tool then return end

    for _, remote in ipairs(tool:GetDescendants()) do
        if remote:IsA("RemoteEvent") then
            -- Burst spam: 5 hits in one frame
            for _ = 1, 5 do
                task.spawn(function()
                    remote:FireServer(target)
                end)
            end
        end
    end
end

-- === 3️⃣ Kill methods
local function softKill(humanoid)
    if humanoid and humanoid.Health > 0 then
        humanoid.Health = 0
    end
end

local function hardBreak(humanoid)
    if humanoid and humanoid.Parent then
        local char = humanoid.Parent
        for _, part in ipairs({"Head", "Torso", "UpperTorso", "LowerTorso"}) do
            local p = char:FindFirstChild(part)
            if p then p:Destroy() end
        end
        humanoid:Destroy()
        char:Destroy()
    end
end

-- === 4️⃣ Toggles
local hardBreakMode = false
local ignoreRange = false

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.H then
        hardBreakMode = not hardBreakMode
        print("💀 Hard Break Mode:", hardBreakMode)
    elseif input.KeyCode == Enum.KeyCode.R then
        ignoreRange = not ignoreRange
        print("🌍 Ignore Range:", ignoreRange)
    end
end)

print("✅ [H] Hard Break toggle | [R] Ignore Range toggle")

-- === 5️⃣ Main unstoppable loop
Heartbeat:Connect(function()
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            local enemyHRP = player.Character:FindFirstChild("HumanoidRootPart")

            if hum and enemyHRP and hum.Health > 0 then
                local dist = (myHRP.Position - enemyHRP.Position).Magnitude
                if ignoreRange or dist <= 30 then
                    spamToolRemotes(enemyHRP)
                    softKill(hum)
                    if hardBreakMode then
                        hardBreak(hum)
                    end
                end
            end
        end
    end
end)

print("🔥 ULTIMATE+ NO COOLDOWN NUKE ENABLED — Frame spam, cooldown null, burst remotes.")
