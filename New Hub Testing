-- Recreate Script Hub UI with updates
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ScriptHubUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 800, 0, 400)
MainFrame.Position = UDim2.new(0.5, -400, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderColor3 = Color3.fromRGB(255, 0, 0)
MainFrame.BorderSizePixel = 2
MainFrame.Parent = ScreenGui

-- LoopBring Button
local LoopBringBtn = Instance.new("TextButton")
LoopBringBtn.Size = UDim2.new(0, 80, 0, 25)
LoopBringBtn.Position = UDim2.new(0, 0, 0, 0)
LoopBringBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
LoopBringBtn.BorderColor3 = Color3.fromRGB(255, 0, 0)
LoopBringBtn.Text = "[LoopBring]"
LoopBringBtn.Font = Enum.Font.SourceSansBold
LoopBringBtn.TextSize = 14
LoopBringBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
LoopBringBtn.Parent = MainFrame

-- Divider Line Under LoopBring
local LBLine = Instance.new("Frame")
LBLine.Size = UDim2.new(0, 2, 1, 0)
LBLine.Position = UDim2.new(0, 80, 0, 0)
LBLine.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
LBLine.BorderSizePixel = 0
LBLine.Parent = MainFrame

-- Top Tabs
local function createTab(name, icon, position)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 80, 0, 25)
    btn.Position = UDim2.new(0, position, 0, 0)
    btn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    btn.BorderSizePixel = 0
    btn.Text = icon .. " " .. name
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 18
    btn.TextColor3 = Color3.fromRGB(0, 255, 0)
    btn.Parent = MainFrame

    -- Divider between buttons
    local divider = Instance.new("Frame")
    divider.Size = UDim2.new(0, 1, 0, 25)
    divider.Position = UDim2.new(0, position + 80, 0, 0)
    divider.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    divider.BorderSizePixel = 0
    divider.Parent = MainFrame
end

createTab("Tips", "🧐", 85)
createTab("Main", "💀", 165)
createTab("Misc", "🔧", 245)
createTab("Alternative\nHubs", "🌐", 325)

-- TDW Title Left Panel Extension
local TitlePanel = Instance.new("Frame")
TitlePanel.Size = UDim2.new(0, 25, 1, 0)
TitlePanel.Position = UDim2.new(0, -25, 0, 0)
TitlePanel.BackgroundTransparency = 1
TitlePanel.Parent = MainFrame

local sideLabels = {"T", "D", "W"}
for i, letter in ipairs(sideLabels) do
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0, 25, 0, 50)
    label.Position = UDim2.new(0, 0, 0, 50 + (i - 1) * 60)
    label.Text = letter
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 24
    label.TextColor3 = Color3.fromRGB(255, 0, 0)
    label.BackgroundTransparency = 1
    label.Parent = TitlePanel
end

-- Divider between TDW and script area
local VerticalDivider = Instance.new("Frame")
VerticalDivider.Size = UDim2.new(0, 2, 1, 0)
VerticalDivider.Position = UDim2.new(0, 0, 0, 0)
VerticalDivider.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
VerticalDivider.BorderSizePixel = 0
VerticalDivider.Parent = MainFrame

-- Right Side Info Panel Toggle Button
local InfoToggleBtn = Instance.new("TextButton")
InfoToggleBtn.Size = UDim2.new(0, 25, 0, 25)
InfoToggleBtn.Position = UDim2.new(1, -30, 0, 0)
InfoToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
InfoToggleBtn.BorderColor3 = Color3.fromRGB(255, 0, 0)
InfoToggleBtn.Text = ">"
InfoToggleBtn.Font = Enum.Font.SourceSansBold
InfoToggleBtn.TextSize = 18
InfoToggleBtn.TextColor3 = Color3.fromRGB(0, 255, 0)
InfoToggleBtn.Parent = MainFrame

-- Right Side Info Panel
local InfoPanel = Instance.new("Frame")
InfoPanel.Size = UDim2.new(0, 180, 1, 0)
InfoPanel.Position = UDim2.new(1, 0, 0, 0)
InfoPanel.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
InfoPanel.Visible = false
InfoPanel.Parent = MainFrame

InfoToggleBtn.MouseButton1Click:Connect(function()
    InfoPanel.Visible = not InfoPanel.Visible
end)

local Username = Instance.new("TextLabel")
Username.Size = UDim2.new(1, 0, 0, 20)
Username.Position = UDim2.new(0, 0, 0, 5)
Username.Text = "User:\nunknownhumanbeingfr"
Username.TextWrapped = true
Username.Font = Enum.Font.SourceSansBold
Username.TextSize = 14
Username.TextColor3 = Color3.fromRGB(0, 255, 0)
Username.BackgroundTransparency = 1
Username.Parent = InfoPanel

local Avatar = Instance.new("ImageLabel")
Avatar.Size = UDim2.new(0, 80, 0, 80)
Avatar.Position = UDim2.new(0.5, -40, 0, 30)
Avatar.BackgroundTransparency = 1
Avatar.Image = "rbxassetid://17174555482"
Avatar.Parent = InfoPanel

local FPS = Instance.new("TextLabel")
FPS.Size = UDim2.new(1, 0, 0, 20)
FPS.Position = UDim2.new(0, 0, 0, 120)
FPS.Text = "FPS: 44"
FPS.Font = Enum.Font.SourceSansBold
FPS.TextSize = 18
FPS.TextColor3 = Color3.fromRGB(0, 255, 0)
FPS.BackgroundTransparency = 1
FPS.Parent = InfoPanel

local Ping = Instance.new("TextLabel")
Ping.Size = UDim2.new(1, 0, 0, 20)
Ping.Position = UDim2.new(0, 0, 0, 145)
Ping.Text = "Ping: 93ms"
Ping.Font = Enum.Font.SourceSansBold
Ping.TextSize = 18
Ping.TextColor3 = Color3.fromRGB(0, 255, 0)
Ping.BackgroundTransparency = 1
Ping.Parent = InfoPanel
